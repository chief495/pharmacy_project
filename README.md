# PharmaMonitor - Система мониторинга наличия лекарств

## Описание проекта

PharmaMonitor — это веб-приложение на Django для мониторинга наличия лекарственных препаратов в аптечных сетях. Пользователи могут:

- Искать препараты по названию, МНН или производителю
- Просматривать наличие и цены в различных аптеках
- Подписываться на email-уведомления о появлении препаратов в наличии
- Получать информацию об аналогах препаратов

## Технологический стек

- **Backend**: Django 4.2+
- **База данных**: SQLite (для разработки), PostgreSQL (для production)
- **Frontend**: Bootstrap 5, HTML5, CSS3
- **Email**: Django Email Backend (настраивается через переменные окружения)

## Основные возможности

### Для пользователей:
- 🔐 Регистрация и вход по email
- 🔍 Поиск препаратов
- 📊 Просмотр наличия и цен в аптеках
- 📧 Email-уведомления о наличии препаратов
- 🔔 Управление подписками на препараты
- 💊 Просмотр аналогов препаратов

### Для администраторов:
- 📝 Управление препаратами, аптеками и сетями
- 📈 Просмотр истории цен
- 👥 Управление пользователями и подписками
- 📊 Административная панель Django

## Установка и запуск

### Требования
- Python 3.8+
- pip
- virtualenv (рекомендуется)

### Шаги установки

1. **Клонируйте репозиторий** (если проект в git):
```bash
git clone <repository-url>
cd pharmacy_project
```

2. **Создайте виртуальное окружение**:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env` в корне проекта**:
```env
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Email settings (опционально)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@pharmamonitor.com
SITE_URL=http://localhost:8000
```

5. **Примените миграции**:
```bash
python manage.py makemigrations
python manage.py migrate
```

6. **Создайте суперпользователя**:
```bash
python manage.py createsuperuser
```
При создании суперпользователя используйте email вместо username.

7. **Загрузите тестовые данные** (опционально):
```bash
python manage.py loaddata fixtures/initial_data.json  # если есть fixtures
```

8. **Запустите сервер разработки**:
```bash
python manage.py runserver
```

9. **Откройте в браузере**:
- Главная страница: http://127.0.0.1:8000/
- Админ-панель: http://127.0.0.1:8000/admin/

## Структура проекта

```
pharmacy_project/
├── drugs/                  # Основное приложение
│   ├── models.py          # Модели данных
│   ├── views.py           # Представления
│   ├── forms.py           # Формы
│   ├── admin.py           # Административная панель
│   ├── urls.py            # URL-маршруты
│   ├── templates/         # Шаблоны
│   └── management/        # Management команды
│       └── commands/
│           └── send_availability_notifications.py
├── pharmacy/              # Настройки проекта
│   ├── settings.py        # Настройки
│   ├── urls.py            # Главные URL
│   └── wsgi.py            # WSGI конфигурация
├── templates/             # Общие шаблоны
│   └── base.html
├── static/                # Статические файлы
├── media/                 # Медиа файлы
├── db.sqlite3             # База данных (не в git)
├── .env                   # Переменные окружения (не в git)
├── requirements.txt       # Зависимости
└── README.md             # Этот файл
```

## Основные модели данных

1. **CustomUser** - Пользователи с email-аутентификацией
2. **Drug** - Препараты (лекарства)
3. **PharmacyNetwork** - Аптечные сети
4. **Pharmacy** - Конкретные аптеки
5. **Availability** - Наличие препаратов в аптеках
6. **UserSubscription** - Подписки пользователей на препараты
7. **Analogue** - Связи между препаратами-аналогами
8. **PriceHistory** - История изменения цен

## Management команды

### Отправка уведомлений о наличии препаратов

```bash
# Отправить уведомления всем подписчикам
python manage.py send_availability_notifications

# Отправить уведомления для конкретного препарата
python manage.py send_availability_notifications --drug-id 1
```

Рекомендуется настроить cron или celery для автоматической отправки уведомлений.

## Настройка email

Для отправки реальных email-уведомлений настройте переменные окружения в `.env`:

```env
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@pharmamonitor.com
```

Для Gmail используйте пароль приложения вместо обычного пароля.

## Развертывание на PythonAnywhere

1. Создайте веб-приложение на PythonAnywhere
2. Загрузите код через git или файлы
3. Создайте виртуальное окружение и установите зависимости
4. Настройте `.env` файл с production настройками
5. Выполните миграции
6. Соберите статические файлы: `python manage.py collectstatic`
7. Настройте WSGI файл
8. Установите `DEBUG=False` в `.env`

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Создано для курсовой работы по Django.
